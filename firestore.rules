rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /planners/couple-main {
      allow read, write: if request.auth != null
        && request.auth.token.email == "abcd1234@naver.com";
    }
  }
}
